# Static files should be served directly, not processed by React Router
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200
/*.xml /:splat.xml 200

# SPA fallback - all other routes go to index.html
/* /index.html 200